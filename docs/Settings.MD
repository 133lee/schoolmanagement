One honest caveat: I checked for an actual "promote student to next grade" feature — there's a StudentPromotion Prisma model and a bare repository (create/findById only), and an Academic Policy settings page with promotion-criteria toggles, but neither has a service or API route wired up anywhere. So promotion between grades currently happens manually (re-enrolling a student into a new class via the enrollments page), not through any automated engine that reads those policy settings — similar to the Security settings page's "not yet enforced" section